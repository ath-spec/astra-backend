apiVersion: v1
kind: Service
metadata:
  name: astra-api-service
  labels:
    app: astra-backend
spec:
  type: LoadBalancer # Creates an AWS Classic/Network Load Balancer
  selector:
    app: astra-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
